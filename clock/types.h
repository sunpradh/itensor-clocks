#ifndef __CLOCK_TYPES_H
#define __CLOCK_TYPES_H

#include "itensor/all.h"
#include "../utils/all.h"

namespace clocks {
    namespace it = itensor;
    using complex = std::complex<double>;
    using std::string;
    using utils::mod1;
    using utils::Interval;
}

#endif
